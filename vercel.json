{
  "installCommand": "npm install",
  "buildCommand": "PATH=$PATH:./node_modules/.bin npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.js"
    }
  ]
}
